fastapi==0.139.0
uvicorn[standard]==0.32.1
apscheduler==3.11.3
python-dotenv==1.2.2
pydantic==2.13.4
langgraph==1.2.8

# Postgres (mismo patron que hikvsion_get_events)
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# Gmail / Drive (reusa OAuth de n8n)
google-api-python-client==2.198.0
google-auth==2.55.2
google-auth-oauthlib==1.4.0

# LLM + embeddings + vector store
# (pines viejos de anthropic/openai/qdrant-client chocaban entre si por una
# version de httpx compartida mas nueva -> "Client.__init__() got an
# unexpected keyword argument 'proxies'". Estas versiones se probaron juntas.)
anthropic==0.116.0
openai==2.44.0
qdrant-client==1.18.0

# Extraccion de texto de adjuntos
pypdf==6.13.1
pdfplumber==0.11.9
python-docx==1.2.0
mammoth==1.12.0
